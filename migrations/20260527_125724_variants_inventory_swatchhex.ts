import { MigrateUpArgs, MigrateDownArgs, sql } from '@payloadcms/db-postgres'

export async function up({ db, payload, req }: MigrateUpArgs): Promise<void> {
  await db.execute(sql`
   ALTER TABLE "products" ADD COLUMN "inventory_count" numeric;
   ALTER TABLE "product_variants" ADD COLUMN "swatch_hex" varchar;`)
}

export async function down({ db, payload, req }: MigrateDownArgs): Promise<void> {
  await db.execute(sql`
   ALTER TABLE "products" DROP COLUMN "inventory_count";
   ALTER TABLE "product_variants" DROP COLUMN "swatch_hex";`)
}
