import { withPayload } from "@payloadcms/next/withPayload";
import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // Configure Next.js for Payload + storefront here.
};

export default withPayload(nextConfig);
